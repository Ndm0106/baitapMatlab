function [B,C] = Bai64(A)
B[];
C[];
tonguoc=0;
for i=1:length(A)-1
   if(rem(A(i),i))
       tonguoc=tonguoc+1;
   end
end
for i=2:sqrt(A)
    if(
for i=1:length(A)
   if(mod(A(i),1)==0)
       B(i)=A(i);
   elseif tonguoc==A(i)
           C(i)=A(i);
       end
end
B
C
end

